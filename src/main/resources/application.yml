spring:
  datasource:
    dbcp2:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost/rest_with_spring_boot_java?useSSL=false
      username: root
      password: 22092003
  jpa:
    hibernate:
      ddl-auto: update
    # não é necessário especificar explicitamente o dialect
    # properties:
      # hibernate:
        # dialect: org.hibernate.dialect.MySQLDialect
    show_sql: false
    open-in-view: false